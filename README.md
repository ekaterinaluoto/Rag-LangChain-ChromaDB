# 📘 RAG-система на основе LangChain и ChromaDB

Проект демонстрирует создание простой Retrieval-Augmented Generation (RAG) системы
с использованием **LangChain**, **ChromaDB** и **OpenAI Embeddings**.

Реализован полный пайплайн:
загрузка данных → разбиение на чанки → векторизация → сохранение в ChromaDB →
поиск релевантного контекста для ответов LLM.

---

## 🚀 Цель проекта

- Освоить работу с векторными базами данных
- Реализовать базовый RAG-подход
- На практике использовать LangChain + ChromaDB
- Подготовить чистый и безопасный проект для GitHub и портфолио

---

## 🧠 Что реализовано

✅ Загрузка текстовых данных  
✅ Разбиение текста на чанки (`RecursiveCharacterTextSplitter`)  
✅ Генерация эмбеддингов через `OpenAIEmbeddings`  
✅ Создание и хранение векторной базы в **ChromaDB**  
✅ Поиск релевантных документов по запросу  
✅ Использование контекста для генерации ответа (RAG)  

---

## 🛠 Используемые технологии

| Технология | Назначение |
|-----------|-----------|
| **Python** | Основной язык разработки |
| **LangChain** | Оркестрация RAG-пайплайна |
| **ChromaDB** | Векторная база данных |
| **OpenAI Embeddings** | Векторные представления текста |
| **Jupyter Notebook** | Разработка и эксперименты |

---

## 📂 Структура проекта

```bash
.
├── RAG_LangChain_Chroma_clean.ipynb   # Основной ноутбук проекта
├── data/                              # Исходные текстовые данные (опционально)

🔐 Работа с OpenAI API Key

В проекте используется безопасный способ передачи API-ключа.
Ключ не хранится в коде и не попадает в GitHub.
import os
from getpass import getpass

os.environ["OPENAI_API_KEY"] = getpass("Enter your OpenAI API key: ")
✔ Ключ вводится вручную
✔ Нет захардкоженных секретов
✔ GitHub secret scanning не срабатывает

▶ Как запустить проект

1️⃣ Установить зависимости:
pip install langchain chromadb openai tiktoken
2️⃣ Открыть ноутбук:

RAG_LangChain_Chroma_clean.ipynb

в Google Colab или Jupyter Notebook

3️⃣ Ввести OpenAI API key при запросе

4️⃣ Выполнить ячейки последовательно
📎 Пример ключевого кода
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_community.vectorstores import Chroma

splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

documents = splitter.split_documents(raw_docs)

embeddings = OpenAIEmbeddings()

vectordb = Chroma.from_documents(
    documents=documents,
    embedding=embeddings
)
🎯 Результат

В рамках проекта:

реализована полноценная RAG-архитектура

создано локальное векторное хранилище

отработан поиск релевантного контекста

подготовлен чистый GitHub-репозиторий без утечек ключей

Проект может служить базой для:

чат-ботов с базой знаний

QA-систем по документам

корпоративных ассистентов

👩‍💻 Автор

Ekaterina Luoto
Проект выполнен в рамках обучения и практики работы с LLM, LangChain и RAG.
├── screenshots/                       # Скриншоты работы (опционально)
└── README.md
